# Landslide Module Environment Configuration — Meppadi, Wayanad
# Real API credentials for official portals (Leave empty to use verified public endpoints)

# MOSDAC (ISRO) Credentials for INSAT-3DR HEM Rainfall (if registered)
MOSDAC_USER=
MOSDAC_PASSWORD=

# IMD Data Service API Key (if approved institutional account)
IMD_API_KEY=

# GSI NGDR / OCBIS Token (if institutionally cleared)
GSI_OCBIS_TOKEN=

# Open-Meteo Fallback (Open public endpoint, no key required)
OPEN_METEO_BASE_URL=https://api.open-meteo.com/v1/forecast
